# Hermes private integration — publication draft

Static homepage, privacy policy and original Ursa Major / messenger logo. This repository must contain PUBLIC site files only. No OAuth credentials, callback URLs, private reports or session history.

## Before publication
- Review both HTML pages; confirm the public operator name and contact email.
- Verify OpenAI account data controls and Google Limited Use compliance before continuing Google-data processing; do not publish an unverified non-training claim.
- Approve a retention/deletion process, provider disclosures, and current scope descriptions. Replace draft caveats with accurate verified details, add an effective date, and remove review banners ONLY after approval.
- Keep app name Hermes consistent with Google Branding. The original emblem is not an official Nous Research, Google or UltiMaker logo.
- Adding a Google OAuth logo may require brand verification. Publishing a website is not Google verification approval.

## GitHub Pages
Create a repository such as `hermes-integration-info` with ONLY this folder's contents. Upload files, not the ZIP itself. Enable Settings → Pages → Deploy from a branch → main / root. Homepage: https://USERNAME.github.io/hermes-integration-info/ ; privacy: same base + privacy.html (examples, not live URLs).

Prefer a custom subdomain you control, such as assistant.harmanindia.com, if approved by the domain owner. Configure Pages custom domain and DNS according to GitHub documentation; enable HTTPS. Do not modify existing main website DNS. Verify the authorised domain in Google Search Console using a Google Cloud project owner/editor. Google recommends owned domains rather than shared hosting domains for OAuth verification. No CNAME is included until a domain is selected.

The GitHub repository URL is not the homepage: use the published Pages URL. Confirm both pages are publicly accessible without login before entering them in Google Branding.

Logo: assets/logo-512.png (upload), assets/logo-120.png (small), assets/logo.svg (editable vector). No external web fonts, JavaScript, analytics or forms.
